# Berkut SCC - Р‘СЌРєР°РїС‹ (.bscc)

## РћР±Р·РѕСЂ
РњРѕРґСѓР»СЊ Р±СЌРєР°РїРѕРІ РІ РІРµСЂСЃРёРё `1.1.5` РїРѕРґРґРµСЂР¶РёРІР°РµС‚:
- СЃРѕР·РґР°РЅРёРµ Р·Р°С€РёС„СЂРѕРІР°РЅРЅС‹С… `.bscc` (DB-only);
- import/upload `.bscc`;
- РїРѕС‚РѕРєРѕРІРѕРµ СЃРєР°С‡РёРІР°РЅРёРµ `.bscc`;
- РІРѕСЃСЃС‚Р°РЅРѕРІР»РµРЅРёРµ (dry-run Рё СЂРµР°Р»СЊРЅРѕРµ) СЃ РїСЂРѕРіСЂРµСЃСЃРѕРј РїРѕ С€Р°РіР°Рј;
- backup plan, scheduler Рё retention policy;
- РІС‹Р±РѕСЂРѕС‡РЅС‹Р№ СЂСѓС‡РЅРѕР№ Р±СЌРєР°Рї (РёРјСЏ, scope, include_files);
- zero-trust RBAC Рё Р°СѓРґРёС‚ РЅР° РІСЃРµС… backup-endpoint.

## РљРѕРЅС„РёРіСѓСЂР°С†РёСЏ
РћСЃРЅРѕРІРЅС‹Рµ РїРµСЂРµРјРµРЅРЅС‹Рµ РѕРєСЂСѓР¶РµРЅРёСЏ:
- `BERKUT_BACKUP_PATH` - РєР°С‚Р°Р»РѕРі С…СЂР°РЅРµРЅРёСЏ Р±СЌРєР°РїРѕРІ.
- `BACKUP_ENCRYPTION_KEY` РёР»Рё `BERKUT_BACKUP_ENCRYPTION_KEY` - РєР»СЋС‡ С€РёС„СЂРѕРІР°РЅРёСЏ.
- `BERKUT_BACKUP_PGDUMP_BIN` - РїСѓС‚СЊ Рє `pg_dump`.
- `BERKUT_BACKUP_MAX_PARALLEL` - Р»РёРјРёС‚ РїР°СЂР°Р»Р»РµР»СЊРЅС‹С… backup jobs.
- `BERKUT_BACKUP_UPLOAD_MAX_BYTES` - Р»РёРјРёС‚ upload РїСЂРё import.

Р’Р°Р¶РЅРѕ:
- РІ non-dev РєР»СЋС‡ С€РёС„СЂРѕРІР°РЅРёСЏ РѕР±СЏР·Р°С‚РµР»РµРЅ;
- СЃРµРєСЂРµС‚С‹ РЅРµР»СЊР·СЏ С…СЂР°РЅРёС‚СЊ РІ git.

## API (РѕСЃРЅРѕРІРЅРѕРµ)
- `GET /api/backups` - СЃРїРёСЃРѕРє Р±СЌРєР°РїРѕРІ.
- `GET /api/backups/{id}` - РґРµС‚Р°Р»Рё Р±СЌРєР°РїР°.
- `POST /api/backups` - СЃРѕР·РґР°С‚СЊ Р±СЌРєР°Рї (РїРѕРґРґРµСЂР¶РёРІР°РµС‚ `label`, `scope`, `include_files`).
- `POST /api/backups/import` - РёРјРїРѕСЂС‚РёСЂРѕРІР°С‚СЊ `.bscc`.
- `GET /api/backups/{id}/download` - СЃРєР°С‡Р°С‚СЊ `.bscc`.
- `DELETE /api/backups/{id}` - СѓРґР°Р»РёС‚СЊ Р±СЌРєР°Рї.
- `POST /api/backups/{id}/restore` - Р·Р°РїСѓСЃС‚РёС‚СЊ РІРѕСЃСЃС‚Р°РЅРѕРІР»РµРЅРёРµ.
- `POST /api/backups/{id}/restore/dry-run` - dry-run РІРѕСЃСЃС‚Р°РЅРѕРІР»РµРЅРёРµ.
- `GET /api/backups/restores/{restore_id}` - СЃС‚Р°С‚СѓСЃ Рё С€Р°РіРё РїСЂРѕРіСЂРµСЃСЃР°.
- `GET /api/backups/plan` - С‚РµРєСѓС‰РёР№ РїР»Р°РЅ Р°РІС‚РѕР±СЌРєР°РїРѕРІ.
- `PUT /api/backups/plan` - РѕР±РЅРѕРІРёС‚СЊ РїР»Р°РЅ.
- `POST /api/backups/plan/enable` - РІРєР»СЋС‡РёС‚СЊ РїР»Р°РЅ.
- `POST /api/backups/plan/disable` - РѕС‚РєР»СЋС‡РёС‚СЊ РїР»Р°РЅ.

## Maintenance mode
Р’Рѕ РІСЂРµРјСЏ СЂРµР°Р»СЊРЅРѕРіРѕ restore РїСЂРёР»РѕР¶РµРЅРёРµ РІРєР»СЋС‡Р°РµС‚ maintenance mode:
- РѕР±С‹С‡РЅС‹Рµ API РІСЂРµРјРµРЅРЅРѕ Р±Р»РѕРєРёСЂСѓСЋС‚СЃСЏ (`503`);
- endpoint СЃС‚Р°С‚СѓСЃР° restore РѕСЃС‚Р°С‘С‚СЃСЏ РґРѕСЃС‚СѓРїРЅС‹Рј;
- РїРѕСЃР»Рµ Р·Р°РІРµСЂС€РµРЅРёСЏ restore maintenance mode РѕС‚РєР»СЋС‡Р°РµС‚СЃСЏ.

## Р Р°СЃРїРёСЃР°РЅРёРµ Рё С…СЂР°РЅРµРЅРёРµ
- РџРѕРґРґРµСЂР¶РёРІР°СЋС‚СЃСЏ РїСЂРµСЃРµС‚С‹ СЂР°СЃРїРёСЃР°РЅРёСЏ: РµР¶РµРґРЅРµРІРЅРѕ, РµР¶РµРЅРµРґРµР»СЊРЅРѕ, РµР¶РµРјРµСЃСЏС‡РЅРѕ (РЅР°С‡Р°Р»Рѕ/РєРѕРЅРµС† РјРµСЃСЏС†Р°).
- РЈС‡РёС‚С‹РІР°СЋС‚СЃСЏ РІСЂРµРјСЏ Р·Р°РїСѓСЃРєР° Рё РґРµРЅСЊ РЅРµРґРµР»Рё (РґР»СЏ РЅРµРґРµР»СЊРЅРѕРіРѕ РїР»Р°РЅР°).
- РџРѕСЃР»Рµ СЂРµСЃС‚Р°СЂС‚Р° scheduler РѕСЂРёРµРЅС‚РёСЂСѓРµС‚СЃСЏ РЅР° РїРѕСЃР»РµРґРЅСЋСЋ СѓСЃРїРµС€РЅСѓСЋ Р°РІС‚РѕРєРѕРїРёСЋ Рё РЅРµ РІС‹РїРѕР»РЅСЏРµС‚ Р»РёС€РЅРёР№ РЅРµРјРµРґР»РµРЅРЅС‹Р№ Р·Р°РїСѓСЃРє.
- Retention СѓРґР°Р»СЏРµС‚ СЃС‚Р°СЂС‹Рµ Р°СЂС‚РµС„Р°РєС‚С‹ РїРѕ `retention_days`, РЅРѕ СЃРѕС…СЂР°РЅСЏРµС‚ РјРёРЅРёРјСѓРј `keep_last_successful`.

## Р‘РµР·РѕРїР°СЃРЅРѕСЃС‚СЊ
- РџСЂРѕРІРµСЂРєР° РїСЂР°РІ РІС‹РїРѕР»РЅСЏРµС‚СЃСЏ РЅР° СЃРµСЂРІРµСЂРµ РґР»СЏ РєР°Р¶РґРѕРіРѕ endpoint.
- РћС€РёР±РєРё API РЅРµ СЂР°СЃРєСЂС‹РІР°СЋС‚ РІРЅСѓС‚СЂРµРЅРЅРёРµ РїСѓС‚Рё, SQL/stack trace Рё С‚РµС…РЅРёС‡РµСЃРєРёРµ РґРµС‚Р°Р»Рё.
- Р”Р»СЏ upload/download/delete РїСЂРёРјРµРЅС‘РЅ hardening: Р»РёРјРёС‚С‹, path validation, РєРѕРЅС„Р»РёРєС‚РЅС‹Рµ Р±Р»РѕРєРёСЂРѕРІРєРё Рё Р±РµР·РѕРїР°СЃРЅС‹Рµ РєРѕРґС‹ РѕС€РёР±РѕРє.

